[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[project]
name = "engulf-clab-vrnetlab"
version = "0.1.0"
description = "Build node images with vrnetlab before engulf-clab deploys"
readme = "README.md"
requires-python = ">=3.14"
license = "MIT"
dependencies = [
    "engulf-api>=1.0,<2",
    "engulf-executable-wrapper-api>=1.0,<2",
    "engulf-clab-ensure-vrnetlab>=0.1,<0.2",
    "PyYAML>=6.0",
]

[project.entry-points."engulf.plugins.v1.goal.v1.org_engulf_executable_wrapper"]
"dev.karel.engulf_clab.vrnetlab" = "engulf_clab_vrnetlab:plugin"

[project.entry-points."engulf.plugins.v1.application.engulf_clab"]
"dev.karel.engulf_clab.vrnetlab" = "engulf_clab_vrnetlab:plugin"

[tool.hatch.build.targets.wheel]
packages = ["src/engulf_clab_vrnetlab"]

[tool.hatch.build.targets.sdist]
include = [
    "/src",
    "/tests",
    "/AGENTS.md",
    "/LICENSE",
    "/README.md",
    "/pyproject.toml",
]

[tool.ruff]
target-version = "py314"
line-length = 100

[tool.mypy]
python_version = "3.14"
strict = true
files = ["src/engulf_clab_vrnetlab"]
