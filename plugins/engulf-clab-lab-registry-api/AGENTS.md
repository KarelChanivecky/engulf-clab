# API package instructions

- Keep the contract implementation-neutral and records immutable.
- Do not add consumption metrics or another feature's private state.
- Preserve the context name, registry plugin ID, and absolute-path validation.
- Run contract tests, Ruff, mypy, and direct-consumer tests after changes.
