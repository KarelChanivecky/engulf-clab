# engulf-clab-ensure-checkout

Shared managed-checkout support for the Containerlab and vrnetlab Engulf
plugins. It selects a valid configured checkout when present, otherwise safely
clones a configured repository into user-scoped Engulf state.

This is a support library, not a discoverable Engulf plugin.
