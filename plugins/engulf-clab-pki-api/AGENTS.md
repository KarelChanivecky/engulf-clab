# Agent instructions

- Preserve the dependency-neutral, immutable projection contract.
- Keep host and container paths paired and node-authorized.
- Do not add catalog parsing, certificate parsing, topology mutation, or image-family behavior.
- Treat the context ID and public field meanings as stable API.
