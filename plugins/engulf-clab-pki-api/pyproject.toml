[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[project]
name = "engulf-clab-pki-api"
version = "2.0.1"
description = "Immutable node-PKI projection contract for engulf-clab integrations"
readme = "README.md"
requires-python = ">=3.14"
license = "MIT"

[tool.hatch.build.targets.wheel]
packages = ["src/engulf_clab_pki_api"]

[tool.hatch.build.targets.sdist]
include = ["/src", "/tests", "/AGENTS.md", "/CONTRIBUTING.md", "/LICENSE", "/README.md", "/USAGE.md", "/build.sh", "/pyproject.toml"]

[tool.ruff]
target-version = "py314"
line-length = 100

[tool.mypy]
python_version = "3.14"
strict = true
files = ["src/engulf_clab_pki_api"]
