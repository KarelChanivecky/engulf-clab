server {
    listen 443 ssl default_server;
    server_name cross.dmz.demo.test;
    include /opt/eclab-pki/state/nginx/identity.conf;

    location / {
        default_type text/plain;
        return 200 "eclab Fedora nginx using a cross-signed chain\n";
    }
}
